# mall-v3 upgraded to botocore >= 1.32.0 — EKS Pod Identity now supported.
# All resources removed so Terraform will destroy them on next apply.
generate_hcl "_terramate_generated_main.tf" {
  content {}
}
