# Mall MySQL Proxy
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mall-mysql-proxy
  namespace: default
  labels:
    app: mall-mysql-proxy
    service: mall
    component: mysql-proxy
spec:
  serviceName: mall-mysql-service
  replicas: 1
  selector:
    matchLabels:
      app: mall-mysql-proxy
  template:
    metadata:
      labels:
        app: mall-mysql-proxy
        service: mall
        component: mysql-proxy
    spec:
      containers:
        - name: mysql-proxy
          image: alpine/socat
          command:
            - socat
            - TCP-LISTEN:3306,fork,reuseaddr
            - TCP:mall-rds.dev.acme.example:3306
          ports:
            - containerPort: 3306
              name: mysql
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          securityContext:
            runAsUser: 0
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
      tolerations:
        - key: "node-group-type"
          operator: "Equal"
          value: "base"
          effect: "NoSchedule"

---
# Mall MySQL Service
apiVersion: v1
kind: Service
metadata:
  name: mall-mysql-service
  namespace: default
  labels:
    app: mall-mysql-proxy
    service: mall
spec:
  selector:
    app: mall-mysql-proxy
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
  type: ClusterIP
